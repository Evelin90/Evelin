// összefűzés ($"string {érték, kifejezés} string ")
/*
bool runLoop = true;
int summa = 0;
int numberCount = 0;

while (true)
{
    string line = Console.ReadLine();
    bool success = int.TryParse (line, out int number);

    if (success)
    {
        summa += number;
        numberCount++;
    }
    else
    {
        break;
    }
}
float mean = (float)summa/ numberCount;

Console.WriteLine($"Átlag: {mean}");


double pi = Math.PI;
const float pi2 = 3.14f;    // zárolja a változó értékét
float pi3 = (float)Math.PI;
double val = Math.Pow(5, 6);     //öt a hatodikon
double sqrt = Math.Sqrt(56);    // négyzetgyök
double sq = Math.Pow(5, 1 / 6f);    // többszörös gyökvonás

*/

